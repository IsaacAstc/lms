{
  "rules": {
    ".read": false,
    ".write": false,

    "games": {
      "$pin": {
        ".read": true,

        "players": {
          "$pid": { ".write": true }
        },
        "answers": {
          "$q": {
            "$pid": { ".write": true }
          }
        },

        ".write": "auth != null"
      }
    },

    "logi": {
      "$courseId": {
        "chat": {
          ".read": true,
          ".write": "auth != null",
          "$msgId": {
            ".write": "!data.exists()",
            ".validate": "newData.hasChildren(['name', 'text', 'atMs']) && newData.child('name').isString() && newData.child('name').val().length >= 1 && newData.child('name').val().length <= 20 && newData.child('text').isString() && newData.child('text').val().length >= 1 && newData.child('text').val().length <= 500 && newData.child('atMs').isNumber()"
          }
        },
        "qna": {
          ".read": "auth != null",
          ".write": "auth != null",
          "$threadId": {
            ".read": true,
            "$msgId": {
              ".write": "!data.exists()",
              ".validate": "newData.hasChildren(['from', 'text', 'atMs']) && (newData.child('from').val() === 'trainee' || (newData.child('from').val() === 'staff' && auth != null)) && newData.child('text').isString() && newData.child('text').val().length >= 1 && newData.child('text').val().length <= 500 && newData.child('atMs').isNumber()"
            }
          }
        }
      }
    }
  }
}
